pre-commit:
  parallel: true
  commands:
    # THE CONCURRENT-LANE COMMIT FENCE (F4102/D83). Opt-in per shell: a lane
    # exports LANE_PATHS (colon-separated globs) and this refuses any commit
    # whose staged set is not a subset. Unset = silent pass, humans unaffected.
    # Runs FIRST-ish (parallel, but cheap) so a sweep dies before lint spends.
    lane-pathspec:
      run: bash scripts/check-lane-pathspec.sh
    # Package-specific linting - only runs when files in that package change
    api-lint:
      glob: 'apps/api/**/*.{ts,tsx}'
      run: bash ./.lefthook/with-node-22.sh yarn --silent lint:staged:api
    mobile-lint:
      glob: 'apps/mobile/**/*.{ts,tsx,js,jsx}'
      run: bash ./.lefthook/with-node-22.sh yarn --silent lint:staged:mobile
    shared-lint:
      glob: 'packages/shared/**/*.{ts,tsx}'
      run: bash ./.lefthook/with-node-22.sh yarn --silent lint:staged:shared

    deps-check:
      glob: '{package.json,yarn.lock,apps/*/package.json,packages/*/package.json}'
      run: bash ./.lefthook/with-node-22.sh yarn --silent deps-check

    # Global formatting for all staged files
    prettier:
      glob: '*.{js,jsx,ts,tsx,json,md,yml,yaml}'
      run: bash ./.lefthook/with-node-22.sh yarn prettier --check {staged_files}

    # (F2505) A dead `root-lint` block used to sit here, disabled with the
    # comment "no root ESLint config". That premise was FALSE: `.eslintrc.js`
    # exists at the repo root and is load-bearing — apps/mobile/.eslintrc.js:2
    # is `extends: ['../../.eslintrc.js']`, and `eslint --print-config` resolves
    # it. Deleted rather than enabled: it had been commented out long enough
    # that nobody intends to turn it on, and a false reason is worse than no
    # block at all.

    secrets:
      run: gitleaks protect --staged --verbose --redact

    # COVERAGE STALENESS FOLLOWS THE COMMIT (2026-08-08). A reviewed COVERAGE
    # row whose file this commit changes is no longer reviewed; the revert is
    # purely mechanical by the tool's own design (only MISSING paths need
    # human judgement, and those still fail CI for it). Left to memory, every
    # active lane forgot it and CI went red four times in one evening — so
    # the commit itself now carries the revert. stage_fixed restages the
    # ledger the tool rewrote.
    # No glob on purpose: the first commit after this hook landed staled a
    # ROOT file's row (lefthook.yml itself) and the glob skipped the revert —
    # any tracked file can hold a review, so every commit runs the ~100ms tool.
    coverage-staleness:
      run: node scripts/coverage-staleness.mjs --apply && git add audit/COVERAGE.md
      stage_fixed: true

# THE PHANTOM-TREE FENCE (F9986). Lanes share one working tree, so `tsc` there
# reads other lanes' UNCOMMITTED edits — a lane can typecheck a state no commit
# will ever reproduce. On 2026-08-09 that put a non-typechecking commit on main
# and blocked every staging deploy while every lane's own tsc was green.
#
# pre-PUSH, not pre-commit: it costs about a minute (three packages in a fresh
# checkout), which is right before main is shared and wrong on every commit.
pre-push:
  parallel: false
  # (jobs: was a list-typed key in this lefthook version and made EVERY push
  # fail with a config error — commands: is the map-typed spelling the
  # pre-commit block already uses.)
  commands:
    clean-tree-typecheck:
      run: bash scripts/check-clean-tree-typecheck.sh
