# Everything red team — campaign ledger (2026-08-19, owner-ordered)

Mandate: red-team all recent plans (superseded-or-completed-ideally), read
and red-team all code end-to-end, map foundational abstractions, land ideal
shapes. Method: subagent breadth reads → decisive claims re-verified
personally → edits only where a harness proves them. Runs alongside the
reground drain + v16 prep (plans/v16-program.md).

## Wave 1 verdicts (landed)

### Legacy plan corpus (55 files >30d old) — DRAINED
52 SUPERSEDED/HISTORICAL (verified against shipped code, not headers);
1 DORMANT-INTENTIONAL (restaurant-profile-revamp — owner-pinned 2026-07-14,
its hours half shipped, resume point intact); 2 zombies, both downgraded on
personal verification:
- poll sentiment: "presence = endorsement" is a DOCUMENTED v1 decision
  (polls.service.ts:1697, ~95% measured, corrected at close) — the
  per-comment polarity upgrade is product backlog (product/polls.md), not
  a defect.
- wave2 checklist micro-item: "New list" row keep-or-delete — unrecorded
  owner micro-decision, cosmetic.

### Personal read: src/shared + src/config — IDEAL-SHAPED
One env-flag dialect (opt-up vs opt-down separated); derived-index law as
unbypassable base class with schema-enumerated membership; invariant
registry doctrine correct (mechanism+mutation pairs, shrink toward
structure). QUEUED IMPROVEMENT: partial-collapse detection for derived
rebuilds — bank previous run's output count as the expectation (the class
itself names this as the missing piece; zero-output scream is blind to
1-row-from-17k).

## Open lanes (running)
- Recent-plans verification agent (fanned out 4 sub-readers).
- Code deep reads: reddit-collector+scoring; entity layer; external-
  integrations. Waves 2+: search (read-only, ⭐05 lane), polls/identity/
  enrichment/places/signals/user-lists/home/photos/billing/ops, scripts/,
  prisma migrations, mobile (findings-only; map untouchable).
- Reground drain (~3.7k remaining at last check) → tombstones → lifecycle
  archival → cost-reconcile → staging deploy → DB push → registry push →
  estimate hash to owner.

## Fix wave 1 (commit 720b0b8dc) — verified-then-fixed

Collector/scoring: tombstone-sweep SQL/TS alias mismatch (rebuild set was
always empty); batch ingest now marks the rescorer dirty (was
submission-time only — hours early); unknown created_at floors to the
ANCIENT sentinel (was fabricated NOW, inflating room mass); sentinel
tombstones rank below real gate judgments in the A(τ) latest-wins reader
(my P7 change had let a later 404 evict gate-kept docs). Rehearsal
quarantine sealed: all five creation probes carry the visibility
predicate, replayDateRange gets sibling parity, rejection DELETES
rehearsal-born surfaces instead of minting false 'deprecated' memory on
live entities, merge-fold carries born_extraction_run_id. Version law
restored: label sweep >= → =, dish-knowledge lt → not-equal (rollback
re-opens). canonicalDomain writes route through identityDomain (denylist
extended: squarespace/wixsite/weebly/yelp/opentable/resy/sevenrooms), 214
polluted rows scrubbed, RED-capable trust spec added.

Downgraded on verification: one-ballot corpus skew = deliberate
activation-epoch sequencing (backfill script staged), NOT a defect;
"market rate-sum pooling" deferred to docket (no market has 2 communities
yet — measure before redesigning).

## Standing docket additions
- Integrations D1/D2/D3 (one batch-job terminalizer that always meters;
  embed/cache under the campaign gate) — next fix wave.
- D4/D5 policy questions for OWNER: @Optional spendCampaigns on scripts;
  D149 user-path bypass keying on PROCESS_ROLE=api which scripts share.
- entity_match memory forfeited by the R14 cosmetic version bump (v1 rows
  invisible; population currently zero — note for bump discipline).
- Dedupe lane budget gate (G2) BEFORE the flag flips post-activation.
- Rising-under-thread-share incoherence (probe-only dial); market pooling
  semantics; stopwords list → judged vocabulary (G1); dup activeSupport
  predicate (G3); probe rows in ledger accounting (D6); non-terminal batch
  status constant (D10); derived-index partial-collapse prior.
- Poll-supply rederivation plan: entirely unexecuted (5 live bugs, 16,226
  poisoned rows) — owner sequencing needed.
