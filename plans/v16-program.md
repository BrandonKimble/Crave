# The v16 program — run plan and everything after (2026-08-19)

Owner mandate: full coordination delegated; one-time-forever enrichment
spend authorized and FIRED (see below); archive load deferred indefinitely;
after v16 we re-audit the database again with fresh eyes. This file is the
standing program. Coordinator = this session (⭐04); search-side = ⭐05.

## Ground truth at time of writing

- Prompt+schema pairing FROZEN (text as of 4aa1937e3), certified 103/105 ×3
  with only the two documented band members flaky; es 90.0 / vi 91.6
  baseline-exact (⭐05, fresh); all gates green both sides.
- Fleet residuals ALL CLOSED: gate re-open (config-scoped verdicts),
  vocabulary + dish-knowledge version ledgers, word_role profile (⭐05),
  scan widened to _judge-suffix callers.
- Full API suite green (2,186 tests); `yarn invariants` 39/39; NIGHTLY
  invariants rung live (scheduled task `nightly-invariants`, 03:30,
  diagnose-never-patch; PUSH-on-red — phone notification + untracked
  INVARIANTS-RED.md at repo root until a green run verifies the fix;
  silent when green).
- RETRO SHADOW SWEEP EXECUTED (SD-1/SD-2): 4,510 shadow-banked surfaces
  deprecated + 4,116 shadow-minted entities archived (witnesses bơ/'bo'
  dead; acc-02 went green; ⭐05's loc-05 fixture is corpus-stale — their
  re-carrier). Scores rebuilt.
- REGROUND FIRED (pre-v16 by owner authorization + ⭐05 co-analysis:
  grounding survives re-extraction; junk fails the chooser; v16 diff
  judges a clean venue population). Tranche of 100 measured 56% recovery;
  full drain of ~4,300 running; estimate ~$120-150 all-in. Follow-ups
  bound to the drain: CLOSED_PERMANENTLY tombstones, the ruled
  no-match lifecycle archival (attempted-and-failed ⇒ not searchable),
  cost-reconcile.

## Phase 0 — remaining prep (this session, after the drain)

1. Tombstone + lifecycle archival + `cost-reconcile.sh`.
2. Deploy current main to STAGING (`deploy.sh --env staging`).
3. `push-local-db-to-staging.sh` — staging inherits the enriched,
   swept corpus.
4. `reextract.sh push` the frozen candidate → registry version (expect 11).
5. `reextract.sh estimate all <version>` → manifest + hash to OWNER.

## Phase 1 — the v16 shadow (fires on owner's hash + word)

`reextract.sh shadow` against staging, ACTIVATE=false, crons stay on,
quiescence verified first. Watch to full batch-queue drain. Budget: the
pre-approved $30 envelope; campaign closed (complete-campaign.ts) whether
activated or rejected.

## Phase 2 — fix-verification (named checklists, not vibes)

Diff review (`reextract.sh diff`) + the specific claims we must see hold:
- Rhino's five landed changes (article deletion, filler principle,
  punctuation-as-observed incl. &-identity, C.1 three-gate + dish
  inheritance, fit assertion) visible in corpus-wide behavior.
- Best-shorthand class: no bare generic-word restaurant mints anywhere in
  the v16 output (P1b's invariant at corpus scale).
- v13 shadow's two measurement checks: praise-mass shift (ANSWER TEST +
  F.1 carriers × 2.0 praise weight → measure score movement), curated-list
  attribute thinning rate.
- Vietnamese junk classes (bơ-class) stay dead; es/vi golds on v16 output.
- Shadow hygiene: the rehearsal sandbox must show ZERO live-side writes
  (the retro sweep found 8,626 pre-rehearsal leaks; the sandbox exists so
  the count is now structurally zero — verify, don't assume).

## Phase 3 — fresh-eyes extraction audit (the rhino method, on real output)

Everything on the table, not just what we fixed: sample real v16
mention rows against their source text in both directions (loss + junk),
provenance per anomaly, model's-shoes rederivation for any prompt defect
found. Owed debts folded in: C.3 parent-category gold pins; census batch 2;
walkthrough section F's unanswered question (should any small lanes merge /
receive conversation context — per-lane verdict in writing). Then the
database re-audit the owner ordered.

## Phase 4 — activation epoch (only after review closes; owner GO)

One calibration epoch, in order: activate (dry-run → --reviewed --execute)
→ GC dry-run → execute → anchor-audit clean → prompt-activate → one-ballot
backfill --apply → market-rooms + activity-floor calibration land together
→ KNOWN_DRIFT deaths (re-mirror the item description quote, re-certify the
aligned pairing) → ⭐05 flip-moment ping (K2) → workers redeployed →
cost-reconcile. Rollback stays possible until explicit discard.

## Phase 5 — post-activation docket (sequenced, owners assigned)

1. Activation GC re-runs the dead-run sweep logic on the superseded
   generation (mine).
2. M3 generic-word sweep at activation vocabulary state (mine).
3. Dedupe un-gate (DEDUPE_JUDGE_LANES_ENABLED) + docket: chili triplicate,
   179 duplicate pairs (mine) ∥ retraction lane (⭐05).
4. Language wave (⭐05): full zh vocabulary (~12k concepts), pinyin layer,
   zh gold corpus + flow gate; my calibration-law audit of those runs.
5. Knowledge-attributes pilots (co-due with the wave, owner-ratified):
   consumable pilot → grading gate → meal-timing text (⭐05 co-sign) →
   enumeration pilots → owner continue-or-kill per pilot.
6. Walkthrough with owner: the pruned agenda (praise-weight ruling waits
   for the corpus-complete bake-off; pilot gates; anything Phase 3 surfaces).

## Parked, with explicit triggers

- Archive load (Austin 2024-25 + NY full): owner-deferred. Trigger = owner.
  When it fires: coverage instrument (docs/month gap alarm) ships WITH it,
  and the equation bake-off re-judges AFTER it (dials committed, one config
  away).
- Pre-launch ship checklist (plans/iteration-phase-open-items.md §D):
  trigger = owner declares ideal shape reached. Prod frozen until then.
