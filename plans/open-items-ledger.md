# OPEN ITEMS LEDGER — transition/track effort (2026-08-08)

One page: everything said-we-would that is not yet done, who owns it, what unblocks it.
Maintained by the track session; supersedes scattered mentions.

## A. Owner device sitting (blocks the most; needs sign-in)
1. Toggle-seam A/B verdict (flag lever, e3eebfe64) — feel both arms, rule.
2. Search-flow choreography post-R8 (the map/sheet reveal worry) — five mouths.
3. Strip/page-switch choreography — read in flight; fixes follow its report.
4. press->real-rows re-measure after the card work; polls toggle probe read.
5. Background/resume mid-transition; stale-binary bark live test; world-join
   cry-wolf check; pinch-zoom 429 verification (coalescer live confirm).

## B. Approved work, queued (owner: "take all the time you need")
6. DATA TIER (frontier #1): react-query defaults (staleTime 0, zero persistence),
   SWR policy, persisted hydrate on cold launch. READ RUNNING.
7. PREWARM PAYLOAD (frontier #2): press-down warms structure only — add data
   (+child-push prewarm via cache, not entry). READ RUNNING.
8. IMAGE PIPELINE (frontier #3): no priority/cachePolicy/prefetch/blurhash;
   lists fade-in policy per OA11 reveal law (fade starts AT skeleton drop).
   READ RUNNING.
9. Session restore / snapshot tier (frontier #5-#6) — after 6-8.
10. PollCard diet: peek-row (price after #1 measurement); SVG swap + previewRows
    3->2 are OWNER visual calls, priced but unruled.
11. OA10 migration M1-M5 (view-mode preference, Lists switcher, shared cells) —
    awaiting owner GO (adds a visible control).

## C. Cleanliness residue (mine)
12. Publication bridge deletion (sheetTranslateY/scrollOffset mirrors — live
    riders: scrim, search chrome) + batch-3 policy vestige + dead transport.
    READ RUNNING (kill plan with riders migrated first).
13. Second mounted-body component map in overlays/ — derive from schema.
14. "Residency" name collision (track column vs shell-residency-registry).
15. redteam-abstractions.md: verify banner coverage is complete per-section.
16. Doubleness audit (owner punchlist #3): video frame-diff sweep across all
    switch types — schedule with the next device sitting.

## D. Standing instruments (keep)
- press-anchored [PERF] probes; row A/B lever (?row=bare|full); toggle-seam
  probe + flag; render lane; native C suites; tracksheet scanner (CI).
