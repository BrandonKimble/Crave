# The v16 run and everything after — THE ROADMAP (owner-delegated, 2026-08-18)

Owner mandate: full coordination + spend authority delegated; restaurant
enrichment ruled a one-time-forever spend, approved NOW. No Austin archive
run yet. Everything closed in the from-scratch ideal shape; owner is asked
for input only at the named checkpoints below.

## PHASE 0 — NOW, pre-shadow (in flight tonight)
- [~] REGROUND CAMPAIGN (owner-authorized spend, ~$183 @ measured
      $0.045/loc): grounding attempts on all 4,057 ungrounded actives →
      failures archived per the ruled lifecycle law (mention re-attachment
      through the verdict ledger). Fired via ⭐04's P8 machinery (asked;
      first right theirs, mine if they defer). UNBLOCKS: venue-axis
      semantic flip, full button row, clean venue population for the v16
      diff review.
- [~] Invariant-cluster close-out (agent): A9a fold_version stamps at the
      two raw re-key sites; 12b usageCaller completeness invariant; 12d
      worker-boot budget-owner refusal; source-table row-collapse alarm
      (the 08-16 wipe's structural answer).
- [~] Horizontal-scroll shortcut row (agent): container + chips-as-data
      abstraction; "All" sole member until gates open.
- [ ] ⭐04 tonight (theirs, text-frozen): VOCABULARY_PROMPT_VERSION →
      fingerprint; dish-knowledge due predicate versioned.
- [ ] OWNER CHECKPOINT ①: nightly-invariants rung yes/no (⭐04 presenting
      the two clean-tree breaks as evidence; my +1 on record).

## PHASE 1 — THE v16 SHADOW (fires on owner's word; both GO-checklists green)
Same-corpus shadow (~$30, pre-approved) under the P6 sandbox: every write
rehearsal-quarantined, one atomic activation flip, byte-identical-derived-
tables acceptance already 4/4. Frozen pairing law in force (prompt as of
4aa1937e3 + schema HEAD; description edits = prompt edits).
- OWNER CHECKPOINT ②: the shadow DIFF REVIEW (the choreography that caught
  v7). Exhaustive-audit doctrine applies — no 12-doc samples.

## PHASE 2 — ACTIVATION + the calibration epoch
- Atomic flip (P6). ⭐04 pings the K2 flip moment → my ONE re-baseline of
  all standing instruments with attribution (coverage-truth, gold gates,
  browse probe, battery).
- Scoring epoch lands with it (creator-one-ballot, market-rooms backfill).
- DB RE-AUDIT (owner-ordered): fresh-eyes read of the post-v16 corpus
  before anything banks on it — the "relook and re-audit the database"
  he named. OWNER CHECKPOINT ③: review its verdict.

## PHASE 3 — CONVERGENCE on the clean corpus
- Dedupe un-gate + docket (chili triplicate, 179 unmasked pairs, Senza
  dup, site-3 identityScope) — verdict-memory-backed from night one.
- Retraction lane (326-collision plan, single-claimant rows) — mine.
- Ghost-lifecycle standing invariant confirmed post-reground (if Phase 0
  archived failures, this is verification, not work).
- VENUE-AXIS SEMANTIC FLIP (T2): "restaurants"/"coffee shops" become
  strict venue-kind searches; provisional bare-'restaurants' browse flag
  retires. Then the BUTTON ROW fills: Restaurants · Bars · Coffee Shops ·
  Bakeries · Food Trucks · Dessert into the scroll container.

## PHASE 4 — THE LANGUAGE WAVE (one campaign, one spend event, estimate up front)
en (L2) + es tail + vi refresh + zh full (~12k concepts; preview live) +
ko; pinyin romanization layer (ruled yes); containment-reads-surfaces
redesign; gender-fold fix; dual-spelling judge clause; word-court third-
facet certification rides co-due (near-free); consumable food/drink pilot
(⭐04, closed-enum, 200-entity double-graded gate) → Food · Drinks buttons
+ meal-timing attributes (co-signed boundary) → time-of-day dynamic slot.
es-01/vi-04 known-reds expected to flip FIXED-KNOWN-RED here.
13g flip re-baseline precedes the wave's vocab runs. Re-baseline all gates
after. OWNER CHECKPOINT ④: wave campaign estimate approval (one number,
all locales).

## PHASE 5 — THE LADDER SESSION (owner + me, the focused pass he asked for)
A1 ratification with everything the campaigns pinned for it: band-2
union-minus-rejects (measured), L1 implementation + decomposed-parts kill,
delete the lexical widener (F3), band-0 rebuild (F1), F8 restaurant-name
decomposition vs No-Name-Burgers arbitration, name-prefix suggestion arm
(floor + response seat), 13c squeeze-claim inheritance, 7b listwise bench
BEFORE corpus-wide commitment, coffee-vs-coffee-shop semantics confirmed.
OWNER CHECKPOINT ⑤: the session itself.

## PHASE 6 — CAPSTONE + PRE-LAUNCH
- C3 synergy capstone: Spanish-post → collect → extract → resolve → bank →
  search-finds-it as a GATE; venue-diversity probe (bars/boba/bakery/truck
  end-to-end); calibration law (line-by-line predicate reading on real
  vi/zh strings).
- TRACK-VISUAL PUNCHLIST (owner-eyes work): strip/choreography, skeleton,
  doubleness — OWNER CHECKPOINT ⑥ (dedicated session; chip to spawn when
  Phase 3-4 quiet down; currently ALSO blocks visually verifying the
  renamed panes).
- Pre-launch ship checklist (section D): honest-empty chip, C2 curation
  recipes as repeatable passes, prod re-hear of v2-era verdicts (tôm),
  full vocabulary rebuild under final prompts, migrations, gates, the one
  clean deploy.
- Austin archive load: DEFERRED indefinitely by owner; relevance-gate
  versioning already in place for whenever.

## STANDING LAWS FOR EVERY PHASE
Executed proof over claimed proof; exhaustive audits before destructive
verdicts; migrations apply at code-commit time; description edits are
prompt edits; spend events print estimates first; instruments must be
able to show RED; the ledger strikes items only with hashes.
