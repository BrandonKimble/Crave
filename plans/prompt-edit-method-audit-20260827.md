# Prompt-Edit Method Audit — 2026-08-27

**Subject:** HOW `collection-prompt.candidate.md` was edited over 2026-08-26 (base `eafdc666d` "rhino rewrite" → `af681538a` → current uncommitted working tree), graded against the canon in `memory/prompt-philosophy-canon.md`. This audits the editor's method, not just the text.

**Scope note:** in the stated range only ONE commit touches the prompt (`af681538a`, one edit) plus a large uncommitted diff (eight distinct edits). Fixture edits in range: N10 (committed), N7 flip + N11–N17 (uncommitted). The base commit `eafdc666d` is itself day-of and supplies the "wording 1" of the sequences examined in §4.

## 1. Per-edit table

| # | Edit (location, quote) | Where | Classification | Verdict |
|---|---|---|---|---|
| 1 | A.1 cond 1 fact-ask: "The verb never decides — the CRITERION does… A reply's CLAUSE that names who has/does/serves the thing is an availability clause… This mutes exactly that clause and no other" | af681538a | **REDERIVATION** (with rider mass) | The wrong turn is located exactly as the canon demands: the trace showed leaks "all carry extra warmth or specificity" and the fix names the discriminator (criterion, not verb) plus the scope (clause, matching the prompt's global clause law). Honest. But the paragraph grew from 2 lines to 16 with three inline examples ("dollar oysters", "espresso buns", "Perla's") — heavier than minimal-form; the examples do teach the class boundary from both sides, so defensible, but this is the token-mass ceiling. |
| 2 | A.1 testimony: "or in the report it introduces **about the same establishment** — a visit list introducing a review of a DIFFERENT place is a credential, A.3's yardstick" | uncommitted | **ABSORBED-BOUNDARY** | Traced (Craft/Otoko credential list, trace §2.4, verbatim quote verified). Correctly cross-wired to the new A.3 yardstick law instead of adding a standalone rule — the rule lives at its decision point. Good. |
| 3 | A PLAN paragraph: "A shortlist built by BROWSING is a plan too, however curated: 'from my list of open tabs, these menus stood out to me' chose names by reading, not eating" | uncommitted | **ABSORBED-BOUNDARY**, additive in shape | Traced (restaurant-week open-tabs case, §2.4, real). The sentence restates the section's own principle (taste requires eating) rather than banning the instance — canon-compliant despite being an insertion, since the paragraph it joins is the decision point. |
| 4 | A.2 hedges: "decent" → "decent for what it is"; "for the mild words ('solid','decent') POSITION decides the direction: standing alone… they lean positive; …offered as a CONCESSION inside a cooler frame they withhold" | uncommitted | **REDERIVATION** | The trace found BOTH failure directions ("breakfast tacos are solid" wrongly dropped, §2.5; "decent hard shell taco" wrongly kept, §2.4) — the edit derives one discriminator (position/frame) covering both, instead of tuning the word list. This is exactly the anti-flip-flop move the canon prescribes. Best edit of the set. |
| 5 | A.3 yardstick: "either the SUBJECT or the YARDSTICK… one law with three familiar costumes" (ask benchmark, credential list, comparison loser) | uncommitted | **REDERIVATION** | Unifies three previously scattered behaviors (ask-anchor already in A, credential in A.1, comparison loser NEW from trace §2.4 Jeffreys) under one named test ("is the verdict ABOUT this, or measured AGAINST it?"). Class-teaching, not instance-listing. Canon-compliant. |
| 6 | B.3 branch phrase: "'the south lamar location'… point at a restaurant whose actual name lives elsewhere in scope… Resolve it through the depth-aware order to that named restaurant" | uncommitted | **ABSORBED-BOUNDARY** | Traced (Jet's thread, §2.6, real). Grounded in a principle ("nobody says X without the brand in the conversation") and in the system's stated grain. One gap: the "no named anchor resolves" fallback branch is asserted, not pinned (§3). |
| 7 | Gate 1 rewrite: head-noun/wrapper-list mechanics → "FOOD OR TERMS. Every word… says one of two things: WHAT FOOD ARRIVES, or THE TERMS OF THE PURCHASE" + named-combo-is-a-dish + writer's-pairing combo + consumed-deal-contents; ripples into C.4, Step D, dessert-list rule | uncommitted | **REDERIVATION** (the big one) | This is a genuine from-scratch reshape, not a patch: the old gate's mechanics (head noun + wrapper list + modifier triage) are replaced by one question a diner would ask, and the three combo costumes fall out of it. Triggered by exactly the tell the canon names — N7 was "the flip-flopping band member" per its own why-field, and the editor diagnosed "the instability WAS the ORDER TEST fighting Gate 1" rather than re-tuning the wording. **BUT it changes settled behavior it did not re-pin: see §3, V8k contradiction — the one real method failure found.** Also note a semantic reversal riding inside: "wagyu tasting menu" flips from redirect-to-`wagyu` to dish-as-spoken, and the emit-as-spoken rule ("'steak combo', never a stripped 'steak'") reverses the old redirect doctrine — a large behavior change carried with less trace evidence than the others (no preflight case shows the redirect harming anyone). |
| 8 | Gate 2: "And it must still EXIST: a dish the text says is gone — 'RIP', 'discontinued'… emits nothing, and the restaurant is untouched (a dead dish is not a closed place)" | uncommitted | **REDERIVATION** | Traced (§2.6 RIP Uchi Candy Bar, real; flagged there as "a defect in the law, not the model"). Placed at its decision point (orderability), phrased as a principle with the place/dish asymmetry made explicit. The quoted tells ("RIP", "discontinued") are illustrative of a stated principle, not a non-exhaustive list standing in for one. Good. |
| 9 | Harness `prompt-ab.ts`: source_map enum-constraint + asserts-nothing filter mirroring `admitWireMention` | uncommitted | **ABSORBED-BOUNDARY** (code) | Both mirror simultaneous pipeline changes (`extraction-pipeline.service.ts`: mechanical no-op + unique-witness pointer repair), each traced to §2.2/§2.3 and matching the trace's own recommendation ("code not prompt"). Honest in intent — but see §3 for the pin-weakening side effect. |
| 10 | cuisine-prompt + dish-knowledge-prompt hybrid-tradition law ("tex-mex → tex-mex AND mexican"; "fajita → tex-mex AND mexican") | uncommitted | **ABSORBED-BOUNDARY** (claims owner ruling) | Traced to §2.6 tex-mex canonicalization question, which the trace explicitly said "needs an owner ruling"; the edit and the dish-knowledge-rule release note both claim the ruling arrived 2026-08-26. Unverifiable from the repo, but the disappoint-asymmetry reasoning (Tex-Mex disappoints no Mexican-tapper; Sichuan may disappoint a Cantonese-wanter) is a real principle, not a patch. **Not pinned — see §3.** |

**Zero pure RIDERs found** — every edit ties to a traced failure or claimed owner ruling. The rider-adjacent mass is inside edits 1 and 7 (example inflation).

## 2. Trace verification

Every quoted failure in the edit rationales was checked against `plans/v17-preflight-trace-20260826.md`; the trace itself is backed by `apps/api/scripts/fixtures/v17-preflight-outputs.json` (28,883 lines of raw model output, committed — the receipts exist).

- N10 (fact-ask deals): trace §2.1, real ("Dollar oysters on Tuesday at Foreign and Domestic"). The fixture's F&D + Perla's text is a synthetic recreation, not the real doc — acceptable per gold-case practice, and the keep-side clause is faithfully modeled on the trace's "killer deal and killer pizza" observation.
- N13 (yardstick): §2.4 credential list + Jeffreys comparison loser — both real, quoted verbatim.
- N14 (browsing shortlist): §2.4 "list of open tabs… stood out to me" — real.
- N15 (solid/decent): §2.5 + §2.4 — real, both directions.
- N11 (dead dish): §2.6 — real.
- N12 (branch phrase): §2.6 Jet's — real; fixture synthesizes a Torchy's analog.
- N16/N17 ("corpus-derived"): the buffalo-cauliflower and brisket-combo texts are NOT in the trace doc; the label "corpus-derived" is honest about that, but these two rest on the editor's assertion alone.
- Commit claim "certified 113-114/115 x3": not re-runnable in this audit; no result fixture for the final series is committed. Taken on faith — flagging that the certification evidence for the COMMITTED af68 state is thinner than for the base rhino commit (x9).

**Verdict: honest tracing throughout.** No edit invents a failure; the two constructed fixtures are labeled as such. The trace doc itself is unusually self-incriminating (it lists its own misses and defensible-either-way cases), which is the opposite of a plausible story.

## 3. Pin coverage gaps

Pinned both sides (good): N10 (mute fa1 / keep fa2 verdict clause), N13 (reject yardsticks / keep Craft), N15 (keep standalone solid / reject concession decent), N11 (reject candy bar / keep Uchi praise), N12 (reject branch string / keep Torchy's+queso), N16 (reject combo / keep both foods), N17 (reject combo / keep judged contents; unjudged-enumeration reject side implicit in notFoods), N7 (keep elvis presley combo / reject bare combo).

**Gaps:**

1. **V8k-wagyu-redirect now contradicts the uncommitted prompt.** V8k pins `items: ["wagyu"]`, `notFoods: ["wagyu tasting menu"]`; the uncommitted Step D parenthetical says "'wagyu tasting menu' — is a DISH per Gate 1 and never lands here", and Gate 1 says emit as spoken, "never a stripped 'steak'". Either the harness has not been re-run against the working tree, or it runs red and that hasn't been reconciled. This is the canon's own v7 lesson (rewrites silently lose unpinned behaviors) in its INVERSE form: a rewrite silently invalidating a still-standing pin. **Must be resolved before certification: re-derive V8k's expectation from the new law and re-run.**
2. **Hybrid-cuisine law is unpinned and contradicts an existing pin.** `cuisine-gold-cases.json` case at line ~37 expects `["tex-mex"]` alone; the new law demands tex-mex AND mexican. No fajita pin in dish-knowledge gold. Neither gold file was touched. Same defect class as #1, in a sibling prompt.
3. **Emit-as-spoken reversal ("steak combo" not "steak") has no dedicated pin** — N7/N16/N17 pin the combo costumes but nothing pins a food-word+terms-word compound emitting as the compound.
4. **Branch-phrase no-anchor fallback** (reference with no named brand in scope → unnamed-venue skip) unpinned.
5. **Harness filter weakens every notRestaurants pin.** The new asserts-nothing filter deletes praise-false attribute-less place mentions before grading, so a model that emits availability rows with `general_praise: false` now passes N10 et al. without the PROMPT ever suppressing them. Production-faithful, yes — but it means the pins now certify the pipeline+prompt composite, and prompt-level regressions in the mute behavior become invisible so long as praise stays false. Worth one comment in the fixture or harness acknowledging the reduced discrimination; nothing acknowledges it today.
6. **Fact-ask keep-side at the ASK level**: N10's keep side is a verdict clause inside a fact ask; no pin protects a pure JUDGMENT ask with a quality criterion phrased factually ("who has GOOD variety?") — the exact boundary sentence the edit added. The edit's most novel sentence is its least pinned.

## 4. The three-wording sequences

- **Availability rule** (rhino: "fact ask → A.2's AVAILABILITY case, emits nothing" → af68: criterion-decides + clause-mute): TWO wordings in window, moving monotonically in one direction (under-specified → discriminator named), each certified between moves, second traced to a fresh-sample failure of the first. **Convergent rederivation, not flip-flop.** The trace even scoped the fix honestly before making it ("prompt defect more than model variance").
- **Trip-report / plan sentence** (rhino reshaped it; uncommitted adds browsing-shortlist): the additions extend one stable principle (eaten vs not-eaten) without ever reversing a case. **Convergent.**
- **Gate 1** (three-gates `af4d6fc71` → wrapper-single-home `eafdc666d` → FOOD-OR-TERMS uncommitted, with N6/N7 flaky across the middle wording per the af68 commit message): this WAS the flip-flop — N7 pinned "combo never a dish" while the ORDER TEST's sameness logic pulled the model the other way, and the af68 commit shipped with the band documented rather than fixed. The canon says a flip-flop tell demands a deeper reshape; the uncommitted rewrite IS that reshape, and its N7 why-field explicitly names the mechanism ("the instability WAS the ORDER TEST fighting Gate 1"). **Method-correct response to a flip-flop — provided the owner ruling it cites is real and the V8k fallout (§3.1) gets reconciled.** Shipping af68 with the band merely "documented" was the one moment the editor tolerated a known internal contradiction inside a "certified" commit.

## 5. Fixture-expectation edits

Only one PRE-EXISTING expectation was changed in the period: **N7** (combo not-a-dish → combo is-a-dish). Was it bent toward passing? The tell would be flipping the pin to whatever the model does with no principle change. Here the flip is (a) attributed to a same-day owner ruling, (b) accompanied by a full Gate 1 rederivation that makes the new expectation lawful, (c) self-documenting about its own history. That is re-derivation, not bending — **conditional on the owner ruling being real, which this audit cannot verify from the repo and which should be one line in the commit that lands it** (the ruling is currently recorded only in a fixture why-field and a rule-release note). All other fixture changes are new cases. The un-updated V8k and cuisine-gold tex-mex expectations are the inverse problem: expectations NOT re-derived when the law moved.

## 6. Overall grade

**B+ — method-honest, one certification-hygiene failure.** The editor followed the canon's hard parts: every edit traces to a real quoted failure or a named ruling; the two flip-flop-shaped problems (solid/decent, Gate 1/N7) were both answered with discriminator-level rederivations instead of word-list tuning; the availability fix names the exact wrong turn before editing; no non-exhaustive lists were added as principles; code fixes went to code per the trace's own triage. The specific shortfalls, in order of severity:

1. The uncommitted Gate 1 rewrite contradicts standing pin **V8k** (and the hybrid law contradicts a **cuisine-gold** pin) with no re-derivation of those expectations — the exact silent-loss class the canon exists for. Blocking.
2. The emit-as-spoken reversal and the "quality criterion phrased as fact" sentence — the two most novel behavior changes — are the least pinned.
3. `af681538a` shipped "certified" with N6/N7 documented-flaky rather than resolved, and with no committed result fixture for its x3 series.
4. The harness asserts-nothing filter silently reduces what the notRestaurants pins can prove; unacknowledged.
5. Token-mass creep in edits 1 and 7 (three examples each) — inside the letter of the canon, at its ceiling.
