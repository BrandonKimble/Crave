// The ResultCard primitive (listdetail-ideal §2d): ONE card per shape
// (restaurant · dish), three variants expressed as declared slots —
// results (no slots), listDetail (note + add-photo + footer), read-only
// (note only). See the header comment in RestaurantResultCard.tsx.
export { default as RestaurantResultCard } from './RestaurantResultCard';
export { default as DishResultCard } from './DishResultCard';
