/**
 * PhotoStrip's tile geometry, in a module the LOADING SKELETON can import.
 *
 * F882: the skeleton restated these numbers by hand and drifted (72-high 4:3
 * tiles against a 96-high 1.1 gallery — a visible resize on every swap). One
 * declaration, two readers: the strip that draws photos and the skeleton that
 * stands in for them.
 */
export const PHOTO_STRIP_TILE_GAP = 6;
export const PHOTO_STRIP_TILE_RADIUS = 10;
