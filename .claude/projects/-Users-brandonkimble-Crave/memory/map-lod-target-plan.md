---
name: map-lod-target-plan
description: Agreed target architecture + staged cutover plan for the search-map pin/dot LOD rework
metadata:
  type: project
---

Target model (agreed 2026-05-29). Fixes jitter/flicker/mass-demotion while keeping live promote/demote DURING gesture and the multi-glyph pin art. See [[map-lod-pin-architecture]], [[map-lod-demotion-root-cause]].

**Split:** JS owns policy (ranked catalog, ranking rules, styling) and pushes the full candidate catalog to native once per results change. Native applies that ranking to the live viewport each camera tick (execution, not decision). Mapbox owns animation + collision.

**Sources:** ONE permanent pin source + ONE permanent dot source + ONE permanent collision source, holding every marker, never mutated after load. (Today: 30 mutating slot sources — the churn/flicker source.)

**Slots:** 30 (+ overflow) orderable layer-groups stay (required because multi-glyph art can't be single-layer). Each binds to its current marker via a layer `filter` (id-match) = a style update, NOT a source delta. Reassignment is a filter swap, only on genuine set change (a trickle), sequenced behind the opacity fade. Source mutation after load → zero.

**LOD:** pure feature-state opacity crossfade (pin↔dot), both resident. Native feature-state opacity stepper ALREADY EXISTS (`livePinFeatureState(opacity:)`, `renderState.currentOpacity/targetOpacity`, display-link animator) — reuse it.

**Visibility:** native screen-space test — project candidate to screen via `point(for:)`, test against the view rect (+px pad) with a horizon/behind-camera guard. Padded lat/lng AABB kept only as a cheap coarse pre-cull. This is what fixes twist/pitch mass-demotion at root.

**Z-order:** keep native `moveLayer` per-slot by screen-Y (not a source mutation).

**Collision:** two role-filtered proxy layers (pin-obstacle: always visible / occupies space; dot+label victims: allow-overlap false, yield to pins). Role flip = filter update (not source mutation). Opacity crossfade is invisible to Mapbox collision, so transitions never fight collision. Mapbox recomputes collision natively per frame (bounded by on-screen symbol count).

**Staged cutover (keeps app working each step):**
- A. Resident sources + filter-bound slots (no selection change yet) — removes source-mutation churn. Fold in a filter-swap-cost spike.
- B. Native screen-space selection per tick; JS stops per-camera selection (pushes catalog only) — fixes twist demotion + live-during-gesture.
- C. Collision role proxies.
- D. Cleanup: delete dead JS `buildMarkerRenderModel`/`viewport_lod` cadence, obsolete role-frame transport, repurpose/remove `padMapBounds`.

**Constraint on process:** map rendering is visually verifiable only on-device by the user — Claude cannot see the screen. Implement incrementally, verify each step against the stability gate + user device check. Watch items: filter-swap cost; on-screen dot collision count.

## STATUS 2026-05-29 — checkpoint committed, full cutover authorized

Rollback anchor commit: **a0ddaa66** ("checkpoint: map LOD stable-membership selection fix + stability gate") on `main`. Roll back the upcoming cutover with `git checkout a0ddaa66 -- <map files>` or reset. Stable-membership selection fix is DONE (gate green, device-confirmed "much better": counts stable, z-order working, labels good).

User directive: go ALL the way to the ideal end state for everything map-related. Requirements: ONE ideal path, **fail hard** (NO fallbacks / backwards-compat / legacy / dead paths), no bloat/messy/huge files, clean architecture. Map is the app's foundation — must be as close to perfect as possible. Don't stop at the contained policy fix; do the resident-source + native-selection + feature-state-crossfade + collision-proxy cutover AND a thorough legacy cleanup.

**Device-reported issues to fix (structurally, via the cutover where possible):**
1. Viewport SNAPS back to the initial viewport the instant every active map gesture ends. (camera controller — camera-intent-arbiter.ts / search-map-native-camera-executor.ts; likely re-applies search/initial camera on gesture end.)
2. Demotion FLASH: a demoting pin fades out fully → flashes back to full opacity → fades out again. (native live pin transition stepper double-trigger — updateLivePinTransitions.)
3. Dot/pin CROSSFADE broken after the first time: dot disappears well before the pin fades in; on demotion the dot doesn't reappear until you zoom out a lot then snaps in; re-promoting needs extra zoom before the pin fades. First crossfade works, subsequent ones don't. (native LOD opacity coordination + resident dot lifecycle — updateLiveDotTransitions / nativeDotOpacity / resident dot keying.)
4. `Parsed collection base mismatch for restaurant-label-collision-source: expected source=31:... got source=31:...` thrown on pin tap → profile flow. (native source-delta base-revision consistency — applyMarkerRoleFamilyDelta / patchParsedFeatureCollection / the label-collision source frame on selection.)

**Root diagnosis (2026-05-29):**
- #1 (camera snap): classic rnmapbox controlled-camera-fights-gesture. The `<Camera>`/writers re-assert the controlled center/zoom/bearing/pitch after a user gesture because the controlled camera store is not synced FROM native `camera_changed` during user gestures, so a re-render snaps back. `camera-intent-arbiter.commit` rejects intents during gesture but the controlled-state re-apply on gesture end is the snap. Separate subsystem from LOD.
- #2 (demotion flash) + #3 (crossfade broken after 1st): native live-transition steppers `updateLivePinTransitions` (~6347) / `updateLiveDotTransitions` (~6497). Transitions key off `nextPresent` (membership) and rebuild `LivePinTransition` with `startOpacity: currentOpacity` whenever `targetOpacity` flips; marginal membership churn during ZOOM (legitimate, since zoom changes in-view contention) drives target 0→1→0 → flash. The dot side (`nativeDotOpacity` + resident dot lifecycle) doesn't crossfade in sync with the pin on repeat cycles. These are transition-QUALITY bugs in the churny per-marker stepper, NOT membership stability (which the policy fix solved). The feature-state-crossfade cutover (both representations resident, opacity-only, no add/remove) replaces this machinery wholesale.
- #4 (base mismatch): thrown at SearchMapRenderController.swift ~11339 / ~11381 when `previousSourceState.sourceRevision != next.baseSourceRevision` — a JS source delta computed against a base revision native doesn't currently hold (JS/native resident-revision divergence) on the selection/profile path for the label-collision source. Symptom of the dual-path + delta-base source-sync complexity; the resident-source cutover (sources never mutated; opacity via feature-state) removes the delta-base contract that can desync.

**Pattern:** #2/#3/#4 are all symptoms of the current churny dual-path source-mutation + per-marker-transition machinery. The ideal cutover (resident sources, single path, feature-state crossfade) fixes them structurally rather than by patching. #1 is independent (camera controlled-state sync).

## CUTOVER IN PROGRESS — single bundle source (2026-05-30)

User authorized move-fast/break-things (no users, not deployed). Driving the resident single-source cutover.

DONE this stretch (JS type-checks clean):
- Render tree (search-map.tsx): 30 per-slot `ShapeSource`s collapsed into ONE `ShapeSource id={STYLE_PINS_SOURCE_ID}`; each of the 30 slot layer-groups (pin shadow/base/fill/rank + interaction + 4 labels) now reads that one source and is scoped by `buildSlotScopedFilter(slotIndex, baseKindFilter)` = `['all', ['==', ['get','nativeLodZ'], slotIndex], <kind filter>]`. New helper `buildSlotScopedFilter`.
- Native live distributor `buildDirectSlotApplyPlans`: rewritten from per-slot-source fan-out to a SINGLE source (`sourceId: state.pinSourceId`), one collection grouped by markerKey (bundle ids already distinct: pin=markerKey, interaction=promotedPinInteractionFeatureId, labels=labelId). Call site + `PreparedDerivedPinAndLabelOutput.pinSourceIds` → `[state.pinSourceId]`.

REMAINING native sites still assuming 30 slot sources (convert per-slot-source model → single `state.pinSourceId`; grep `pinSlotSourceIds`/`slotSourceId(`): lines ~3027-3028 (labelSourceIds/pinInteractionSourceIds = slot sources — labels+interaction now live in the ONE bundle source), 3705/3725 (snapshot/enter path distributes to slot sources — must target pinSourceId), 7507/7747-7750 (live transition feature-state applied to slotSourceId(lodZ) → state.pinSourceId), 8523/8527/9768 (visualAndInteractionSourceIds = slot+dot → singular sources), 8615 (slotIndex<count z-order guard), 10353/10369 (source ownership), 11435 (attribution). Also the ENTER path `applyMarkerRoleFrame` writes pin→pinSourceId but interaction→pinInteractionSourceId and labels→labelSourceId (separate sources the render tree no longer reads) — must write the whole bundle to pinSourceId. Best next step: native build to enumerate exact compile errors (breakage acceptable), fix systematically. The 30 slot LAYER ids (pinVisualStackLayerIds) stay valid for z-order moveLayer; only the SOURCE collapses.

CONVERTED to single source (state.pinSourceId) this stretch:
- `buildDirectSlotApplyPlans` (live distributor) — done.
- `buildSlotApplyPlans` (snapshot/replace distributor) — rewritten single-source; call site (~3724) + `previousPromotedGroupIds` (~3705) → pinSourceId.
- live transition feature-state targets (`transitionSourceId`, `pinPhysicalSourceId`, `labelPhysicalSourceId`) → state.pinSourceId (labels live in the bundle now).
- `visualSourceIds`/`visualAndInteractionSourceIds`/`managedSourceIds` → `[pinSourceId, dotSourceId, labelCollisionSourceId]` (the only 3 rendered sources).
- press-targeting `labelSourceIds`/`pinInteractionSourceIds` → `[pinSourceId]`.
- Both distributors have a single caller each (verified).
- ENTER path: applyMarkerRoleFrame still stages pins→pinSourceId, interaction→pinInteractionSourceId, labels→labelSourceId, but the snapshot reconcile prepare (~3693) READS those and writes the combined bundle to pinSourceId, so the rendered source ends up complete — verify on device.

REMAINING (telemetry/harmless, compile OK): `slotSourceIds:` diag params (~3174/5857/6001/6147), slot topology contract, `slotIndex < pinSlotSourceIds.count` z-order guard (8615 — count still 30 via attach), commit-ack `|| pinSlotSourceIds.contains` (10353/10369), attribution (11435).

### BUILD COMPILES + RUNS. Device test (initial submit): only DOTS rendered, NO pins/labels, runtime error:
`Mapbox [error] Failed to update non-exist feature in GeoJSON source, feature id: <markerKey>` 

ROOT CAUSE — enter-path DOUBLE-WRITE to `pinSourceId`: the non-live enter path runs `applyMarkerRoleFrame` (writes pins→pinSourceId family) AND THEN `reconcileAndApplyCurrentFrameSnapshots` → the snapshot prepare (~3693) re-assembles the bundle and writes it to `pinSourceId` again via `buildSlotApplyPlans`. Because the pin family already populated pinSourceId's collection base, the bundle patch emits an UPDATE for a pin feature the actual (empty) Mapbox source never received → "update non-exist", pins/labels never render (dots use the separate dotSource so they show). The LIVE path (`reconcileAndApplyLiveMarkerRoleOutputs`→`buildDirectSlotApplyPlans`) is single-writer and correct.

FIX DIRECTION (next): unify the enter path to ONE writer of pinSourceId. Cleanest = give the rendered bundle its OWN source id distinct from the pin/interaction/label STAGING sources: applyMarkerRoleFrame keeps staging pin→pinSourceId, interaction→pinInteractionSourceId, label→labelSourceId (all UNrendered), and BOTH distributors + render tree + transitions + source lists target a new `bundleSourceId`. OR (smaller) make the snapshot prepare the SOLE pinSourceId writer (skip applyMarkerRoleFrame's pin apply for the rendered source / reset pinSourceId base before the bundle write / force REPLACE on the enter bundle write since it's non-live). Trace `reconcileAndApplyCurrentFrameSnapshots` desiredPins origin (~4400) + where the prepare plan + the pin-family plan both reach the apply batch and dedupe by sourceId. Live path already proves the model works; this is enter-path plumbing only.

### FIX APPLIED — dedicated rendered bundle source (separate from pin staging)
Root conflation: `pinSourceId` (`STYLE_PINS_SOURCE_ID`) was used as BOTH the in-memory pin STAGING family (markerRenderState/transitions/desiredPins) AND the rendered bundle target — so the bundle write corrupted the staging family's base → "update non-exist". (pinSourceId was always in-memory staging with NO ShapeSource; originally the 30 slot sources were the rendered pin sources.) Fix = give the rendered bundle its OWN id, derived deterministically as `"\(pinSourceId)-bundle"` (no attach param):
- JS search-map.tsx: `RESTAURANT_PIN_BUNDLE_SOURCE_ID = \`${STYLE_PINS_SOURCE_ID}-bundle\``; render tree ShapeSource + all slot layer sourceIDs → bundle id; attach still passes pinSourceId=STYLE_PINS_SOURCE_ID (staging).
- Native: `InstanceState.pinBundleSourceId: String { "\(pinSourceId)-bundle" }` computed prop. Repointed to bundle id: both distributor call sites (buildDirectSlotApplyPlans/buildSlotApplyPlans), their base lookup (previousPromotedGroupIds), PreparedDerivedPinAndLabelOutput.pinSourceIds (×3), visual/managed source lists (×3 → [bundle,dot,collision]), press-targeting label/interaction query sources, live transition feature-state targets (transitionSourceId/pinPhysicalSourceId/labelPhysicalSourceId), commit-ack (`sourceId == pinSourceId || pinBundleSourceId`). LEFT on pinSourceId (correct, staging bookkeeping): derivedFamilyState(pinSourceId) markerRenderState reads, desiredPins, updateLivePinTransitions family, syncMountedSourceState(sourceId: pinSourceId) (feature-state tracking tied to pinFamilyState; actual Mapbox feature-state applied via pinPhysicalSourceId=bundle).
JS type-clean. Native build in flight (/tmp/ios-bundle-build.log). WATCH on device: do pins+labels render on enter now? feature-state mounted-tracking on pinSourceId vs Mapbox feature-state on bundle MAY cause redundant/mismatched setFeatureState (affects fade, not whether pins show) — refine if fades misbehave.

### CUTOVER VERIFIED (checkpoint c12c3702). Pins+dots+labels render across enter/twist/zoom-in/zoom-out; 0 native source errors. Cutover was TOPOLOGY-ONLY so #2/#3 persist (transition machinery still remove/add + uncoordinated dot/pin opacity).

### AUTONOMOUS VALIDATION LOOP (established — use this, don't stop for sign-off):
- Drive: `maestro test <flow>` for basic (tap 'Show best restaurants here' — NOT 'Best restaurants', that's visible-text not a11y), OR `yarn perf:scenario:ios <flow> <name>` (with the IOS_SIMULATOR_NAME='iPhone 17 Pro' IOS_RUN=0 IOS_REQUIRE_OPEN=0 PERF_SHORTCUT_USE_SIMULATOR=1 env + unset device vars) when the perf command lane (animate_map_camera/submit_shortcut_restaurants) is needed.
- SEE: `xcrun simctl io booted screenshot /tmp/x.png` then Read the PNG — I can read pins/dots/labels/colors/z-order. Maestro `takeScreenshot: /tmp/name` works inside flows. Reusable flow: maestro/perf/flows/search-map-validate.yaml.
- ERRORS: `xcrun simctl spawn booted log stream --level debug --predicate 'process == "cravesearch"' > f &` then grep "base mismatch|non-exist feature|Mapbox.*error".
- TEMPORAL (flash/crossfade/snap — single frame can't show): perf contracts / new diagnostic events. Screenshots only prove STEADY-STATE rendering.
- Note: command-lane `animate_map_camera&bearing=` did NOT visibly rotate the map in validate flow — fix the twist to a real gesture or verify bearing applies.

### PER-ISSUE PLAN (build a detector that FIRES on current build, then fix, then confirm it clears):
- #1 camera snap: DONE + VERIFIED (commit 5d96551c). Added `cameraIntentArbiter.syncControlledCameraToUserGesture` + call in map-interaction-controller `handleNativeViewportChanged` (sync mapCenter/zoom/bearing/pitch from live camera while gesture active + as it settles). Verified via Maestro swipe → screenshot (map stays panned, "Search this area" shows). [ORIGINAL ROOT: controlled `<Camera centerCoordinate={mapCenter} zoomLevel={mapZoom}>` (search-map.tsx ~522).] mapCenter/mapZoom are written ONLY by cameraIntentArbiter programmatic intents (use-search-runtime-camera-intent-runtime.ts ~159) — NEVER synced from user gestures, so they hold the stale search camera and re-apply on gesture-end. FIX: in the camera_changed handler (search-map.tsx `handleNativeViewportChangedFromOwner` ~1726 → `nativeViewportChangedHandlerRef.current`), when `isGestureActive`, sync mapCenter/mapZoom/bearing/pitch to the live payload (state owned ABOVE search-map — find where nativeViewportChangedHandlerRef is assigned; the arbiter rejects commits during gesture so set the React state directly). Detector: flow pans then waits idle; log committed-camera vs controlled-camera; assert they match (no revert).
- #4 collision base mismatch: thrown SearchMapRenderController.swift ~11339 (`previousSourceState.sourceRevision != next.baseSourceRevision`) for restaurant-label-collision-source on profile/selection. JS source-store delta base diverges from native. Detector: run pin-selection-profile flow + grep syslog for "base mismatch...restaurant-label-collision-source". Likely pre-existing selection-path source-revision desync; the ideal model may fold collision into the bundle/feature-state.
- #2 flash + #3 crossfade (IN PROGRESS): the pin opacity (updateLivePinTransitions ~6347, bundle source feature-state) and dot opacity (updateLiveDotTransitions ~6497, `nativeDotOpacity` on dot source) are computed in SEPARATE systems with SEPARATE transition maps → not synchronized. So a demoting pin's fade-out and its dot's fade-in don't crossfade (dot appears late/snaps), and marginal membership churn during ZOOM (legit contention change frame-by-frame at the rank-30/in-view boundary) flips a marker's nextPresent true/false → pin targetOpacity 1↔0 → the "fade out → flash full → out". `sourceFeatureOpacityForPlacementPreroll` only forces full during INITIAL reveal (preparingReveal), NOT live re-promote — so the live flash is membership-churn, not preroll. IDEAL FIX: one per-marker LOD crossfade that drives pin opacity↓ and dot opacity↑ together (same start, same duration, opposed), features resident through the fade; demote pin removed from bundle only after fully faded (already), dot resident always. Need to coordinate the two transition systems (shared start time / unified per-marker transition). Plus damp zoom-boundary membership churn (extend the stable-membership policy to be hysteretic across zoom, not just pan). DETECTOR (native diag to add): per-marker opacity trajectory — emit `lod_crossfade_contract` if pin opacity is non-monotonic across a single demotion (flash) OR if dot opacity stays ~0 while pin opacity ∈ (0.05,0.95) during a transition (crossfade gap).
  - DETECTOR BUILT (rebuild in flight /tmp/ios-cf-build.log): `native_live_lod_transition_contract` now emits `flashReversalCount` (pin transition whose startOpacity is mid-range 0.05–0.95 = reversed mid-fade) + `crossfadeGapCount` (pinExitMidFade markers with no dot enter-transition) + `pinExitMidFadeCount`. JS forwards them; parity checker has a "LOD crossfade clean" gate (fail if either >0) in the search_map_lod block. Run LOD/validate flow → which fires tells the mechanism: flashReversal>0 = membership churn during zoom (fix = zoom hysteresis in buildMarkerRenderModel); crossfadeGap>0 = dot not fading in with pin (fix = coordinate dot enter with pin exit).
  - OPACITY EXPR FINDING: pin opacity = `['coalesce', ['feature-state','nativeLodOpacity'], ['get','nativeLodOpacity'], 1]` (search-map.tsx ~2079) — DEFAULT 1 (full). A re-added pin feature renders FULL for a frame if its 0-opacity feature-state/property hasn't applied yet → a 3rd possible flash mechanism (add-before-opacity-set) my reversal detector won't catch; if flashReversalCount stays 0 but flash persists on device, this is it (fix: set opacity property=0 atomically on add, or keep feature resident so no re-add).
### HEALTH SNAPSHOT (post-cutover build, commit 1b43db8b, LOD flow). 5 MAP GATES GREEN: LOD classification (pins=30/dots=6), slot topology, promotion stability (no oscillation, 237 evts), LOD crossfade clean (0 flash/0 gap, 622 demotions), promoted-family alignment (pins=30 interactions=30 labels=120 collisions=30). 4 remaining failures = pre-existing hydration/bus (NOT map). 1 NEW map regression: `map_rendered_label_collision_contract` missing (task #12) — label OBSERVATION query still filters by old labelSourceId; labels render+collide fine, only observation reporting regressed; fix = point observation query's required source at pinBundleSourceId (mirror press-target fix; press query at ~3067 already uses bundle via `labelSourceIds`). Find observation query via scheduleLabelObservationRefresh.

### ISSUE STATUS: #1 FIXED+verified+committed (5d96551c). #2/#3 DETECTOR-CLEAN on current build (pre-cutover repro; need user manual re-test; detectors+gate guard). #4 NOT reproducing (single + cycle profile flows). Camera/cutover/loop all committed.
### REMAINING PLAN: task#12 label-obs fix; Stage B native screen-space selection (the "phone-screen bounds" projection, replaces padded-AABB); Stage C collision role proxies; Stage D legacy purge (dead slot-source machinery; but pinSlotSourceIds still used for slot-LAYER z-order count — keep layer ids, remove dead source refs). Autonomous loop fully established (see above).

- [OLD note] #2 flash + #3 crossfade: THE transition rework. Make pin features RESIDENT (never remove on demote; opacity→0 via feature-state) and coordinate dot opacity↑ with pin opacity↓ (true crossfade). The flash = re-added pin renders a frame at preroll/default opacity (sourceFeatureOpacityForPlacementPreroll); crossfade break = dot not made visible until pin gone. updateLivePinTransitions (~6347) / updateLiveDotTransitions (~6497) / the nativeDotOpacity resident-dot lifecycle. Detector: native per-marker opacity-trajectory diag — emit if pin opacity non-monotonic during a demotion (flash) or if dot opacity stays 0 while pin opacity in (0,1) during a transition (crossfade gap).
